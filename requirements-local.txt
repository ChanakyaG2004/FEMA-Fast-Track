# Local development and long-running backend dependencies. Vercel installs
# requirements.txt only, because its serverless filesystem cannot host the
# persistent Chroma index.
-r requirements.txt
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
langchain==0.3.14
langchain-community==0.3.14
langchain-text-splitters==0.3.4
chromadb==0.5.23
posthog==3.7.4
